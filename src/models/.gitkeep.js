// Model layer - Prisma handles models via schema.prisma
// This directory is reserved for custom model helpers / query abstractions.
//
// Example usage:
//   const prisma = require('../config/database');
//   const findUserByEmail = (email) => prisma.user.findUnique({ where: { email } });
//   module.exports = { findUserByEmail };
